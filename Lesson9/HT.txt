1) Написать функцию, которая возводит в квадрат число, которое мы передаём функции. (помните, названия переменных и должны быть семантически верными).

2) Написать функцию, которая вычисляет сумму чисел в диапазоне, который укажет пользователь.

3) Написать функцию, которая вычисляет сумму всех чисел, картных 3 в диапазоне, который выберет пользователь.

4) Написать функцию, которая сконкатенирует 2 строки, которые передаст ей на вход пользователь.

5) Написать функцию, которая проверят переменную на значение и, если она типа bool, выводит на экран сообщение "this is bool".
Если она типа number - "this is number". Иначе, выводит - "this is another type".

6) Написать функцию, которая вернёт вам вашу переменную * 3 / 5 *1000 / 0.123. Ответ записать в переменную. 
Далее, переменную, в которую записан реззультат, умножьте на 100 и выведите в консоль.